/* Packaged MCP SDK full copy placeholder. Intentionally left as proxy to avoid duplication. */
(function(){
  if (typeof window !== 'undefined' && !window.MCP) {
    var s = document.createElement('script');
    s.src = '/static/mcp-sdk.js';
    document.head.appendChild(s);
  }
})();
