apiVersion: v1
kind: Secret
metadata:
  name: ipfs-cluster-secret
  namespace: ipfs-kit
type: Opaque
stringData:
  # This is a placeholder - in production, generate a random secret and Base64 encode it
  # For example: openssl rand -base64 32
  cluster-secret: "REPLACEME_WITH_ACTUAL_SECRET_GENERATED_BY_OPENSSL"